[project]
name = "worldlens-etl"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "grpcio>=1.82",
    "cloud-sql-python-connector[pg8000]>=1.20",
    "pg8000>=1.31",
]

[tool.setuptools.packages.find]
where = ["src"]
